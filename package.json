{
  "name": "shastainspections",
  "version": "1.0.0",
  "private": true,
  "description": "Shasta Home Inspections — static site on Cloudflare Workers",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^4.42.0",
    "typescript": "^5.9.0"
  }
}
